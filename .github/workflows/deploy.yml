name: Build and deploy frontend to personal server

# Builds the frontend image from THIS fork's root (Vue 2 + Vite + pnpm),
# pushes it to the fork owner's GHCR namespace
# (ghcr.io/<github.repository_owner>/quantdinger-frontend), then SSHes to
# the user's own Linux host and restarts the frontend container via
# `docker-compose.ghcr.yml` (the same compose file used by the backend).
#
# Why we do not reuse the public image:
#   The upstream image is built from OpenByteInc/QuantDinger-Vue at a
#   specific commit hash. If you've edited files under src/, the published
#   image does NOT include your changes. This workflow always builds from
#   YOUR fork's code.
#
# Why this is independent from the backend deploy workflow:
#   Backend and frontend images live in different GHCR namespaces
#   (ghcr.io/<owner>/quantdinger-backend vs quantdinger-frontend). The two
#   deploy workflows are siblings — neither waits for the other.
#
# Triggers:
#   - workflow_dispatch (manual only).
#
# Architecture: linux/amd64 only (matches the user's server, Skylake IBRS
# Xeon). Drop the `platforms:` array from the build step to add arm64
# later.
#
# Why `cancel-in-progress: false`:
#   A `docker compose up -d frontend` interrupted mid-flight can leave the
#   frontend container in a degraded state. We do not want a second
#   dispatch to abort the first.
#
# Required secrets (set in fork repo Settings → Secrets and variables →
# Actions):
#   DEPLOY_SSH_KEY   - ed25519 private key (no passphrase), pubkey pre-installed
#                      on the deploy user's ~/.ssh/authorized_keys.
#   SERVER_HOST      - server hostname or IP.
#   SERVER_USER      - SSH username (recommend a non-root `deploy` user in
#                      the `docker` group, so it can run `docker compose`
#                      without sudo).
#   SERVER_PORT      - optional, defaults to 22.

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Override the GHCR tag (e.g. v5.0.1). Leave blank to auto-tag as 'manual-<short-sha>'."
        required: false
        default: ""
        type: string
      host:
        description: "Override secrets.SERVER_HOST for a one-off run. Leave blank to use the secret."
        required: false
        default: ""
        type: string
      deploy_dir:
        description: "Server-side working directory."
        required: false
        default: "/opt/quantdinger"
        type: string
      prune:
        description: "Run `docker image prune -f` after deployment."
        required: false
        default: "false"
        type: boolean
      dry_run:
        description: "Build and push to GHCR, but skip the SSH deploy step."
        required: false
        default: "false"
        type: boolean

concurrency:
  group: deploy-frontend-${{ github.event.inputs.image_tag || github.sha }}
  cancel-in-progress: false

permissions:
  contents: read    # checkout
  packages: write   # push to fork owner's GHCR namespace

jobs:
  build-and-deploy:
    name: Build, push, and deploy frontend
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Echo run context
        env:
          DEPLOY_HOST_RESOLVED: ${{ inputs.host || secrets.SERVER_HOST }}
          DEPLOY_PORT_RESOLVED: ${{ secrets.SERVER_PORT || '22' }}
          REPO_OWNER: ${{ github.repository_owner }}
        run: |
          {
            echo "### Run context ###"
            echo "repo_owner          = ${REPO_OWNER}"
            echo "actor               = ${{ github.actor }}"
            echo "sha                = ${{ github.sha }}"
            echo "image_tag (input)   = '${{ inputs.image_tag }}'"
            echo "dry_run             = ${{ inputs.dry_run }}"
            echo "prune               = ${{ inputs.prune }}"
            echo "deploy_dir          = ${{ inputs.deploy_dir }}"
            echo "host (resolved)     = $DEPLOY_HOST_RESOLVED"
            echo "port (resolved)     = $DEPLOY_PORT_RESOLVED"
            # SERVER_USER is a secret; do not echo its value.
            echo "user (set via secret)= $([ -n '${{ secrets.SERVER_USER }}' ] && echo yes || echo no)"
          } | tee -a "$GITHUB_STEP_SUMMARY"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Checkout
        uses: actions/checkout@v7

      - name: Log in to GHCR (fork owner namespace)
        uses: docker/login-action@v4.5.2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve image tag and application version
        id: resolve
        env:
          INPUT_TAG: ${{ inputs.image_tag }}
          REPO_OWNER: ${{ github.repository_owner }}
        run: |
          if [ -n "$INPUT_TAG" ]; then
            tag="$INPUT_TAG"
          else
            tag="manual-${GITHUB_SHA::7}"
          fi
          # GHCR requires the repository name lowercase. We lowercase the
          # owner via bash parameter expansion instead of GitHub Actions'
          # `| lower` filter — that filter triggered a schema fallback that
          # hid the workflow name and the Run workflow button in the UI.
          image="ghcr.io/${REPO_OWNER,,}/quantdinger-frontend:${tag}"
          # Manual dispatch ⇒ no real git tag.
          app_version="0.0.0-${GITHUB_SHA::7}"
          git_tag=""
          echo "image_tag=${tag}"   >> "$GITHUB_OUTPUT"
          echo "ghcr_image=${image}" >> "$GITHUB_OUTPUT"
          echo "app_version=${app_version}" >> "$GITHUB_OUTPUT"
          echo "git_tag=${git_tag}"   >> "$GITHUB_OUTPUT"

      - name: Validate external tag exists in GHCR
        if: inputs.image_tag != ''
        env:
          GHCR_IMAGE: ${{ steps.resolve.outputs.ghcr_image }}
        run: |
          if ! docker manifest inspect "$GHCR_IMAGE" > /dev/null 2>&1; then
            echo "::error::Frontend image $GHCR_IMAGE not found in GHCR."
            echo "::error::Either push it first (run this workflow with image_tag blank),"
            echo "::error::or pick a tag that already exists in your fork's GHCR namespace."
            exit 1
          fi

      - name: Build and push frontend image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          build-args: |
            BUILD_REGION=global
            APP_VERSION=${{ steps.resolve.outputs.app_version }}
            GIT_TAG=${{ steps.resolve.outputs.git_tag }}
          tags: ${{ steps.resolve.outputs.ghcr_image }}
          cache-from: type=gha,scope=deploy-frontend
          cache-to: type=gha,mode=max,scope=deploy-frontend
          provenance: mode=max

      - name: Confirm image is reachable in GHCR
        env:
          GHCR_IMAGE: ${{ steps.resolve.outputs.ghcr_image }}
        run: |
          # Retry briefly; GHCR propagation is usually instant but the API can lag.
          for i in $(seq 1 5); do
            if docker buildx imagetools inspect "$GHCR_IMAGE" --raw | grep -q manifest; then
              echo "Image $GHCR_IMAGE confirmed in GHCR."
              exit 0
            fi
            echo "Attempt $i: manifest not visible yet, retrying in 5s..."
            sleep 5
          done
          echo "::error::Image $GHCR_IMAGE not visible after build+push. GHCR propagation issue?"
          exit 1

      - name: Exit early on dry_run
        if: inputs.dry_run
        run: |
          echo "::notice::dry_run=true — skipping SSH deploy. Image ${GITHUB_SHA::7} is in your GHCR."

      - name: Upload deploy script to server
        if: "!inputs.dry_run"
        uses: appleboy/scp-action@v1
        with:
          host: ${{ inputs.host || secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          command_timeout: 2m
          source: ops/deploy/deploy-remote.sh
          target: ${{ inputs.deploy_dir }}/.deploy-frontend.sh

      - name: Run deploy on server
        if: "!inputs.dry_run"
        uses: appleboy/ssh-action@v1
        env:
          IMAGE_TAG: ${{ steps.resolve.outputs.image_tag }}
          GHCR_IMAGE: ${{ steps.resolve.outputs.ghcr_image }}
          DEPLOY_DIR: ${{ inputs.deploy_dir }}
          COMPOSE_FILE: docker-compose.ghcr.yml
          PRUNE: ${{ inputs.prune }}
          REPO_OWNER: ${{ github.repository_owner }}
        with:
          host: ${{ inputs.host || secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          command_timeout: 30m
          command_stop: true
          # appleboy/ssh-action: declare which step-level env vars to forward
          # into the remote shell's environment. Without this, $DEPLOY_DIR et al.
          # are unbound inside `set -u` and the script aborts.
          envs: IMAGE_TAG,GHCR_IMAGE,DEPLOY_DIR,COMPOSE_FILE,PRUNE,REPO_OWNER
          script: |
            set -euo pipefail
            # appleboy/scp-action@v1 quirk: with source being a relative path
            # and target being a non-existent absolute file path, scp-action
            # creates target as a directory and nests source's path inside.
            # Normalize: if target is a dir, find the actual script inside.
            target="$DEPLOY_DIR/.deploy-frontend.sh"
            if [ -d "$target" ]; then
              script_path="$(find "$target" -name 'deploy-remote.sh' -type f | head -1 || true)"
            else
              script_path="$target"
            fi
            [ -f "$script_path" ] || { echo "::error::Cannot find deploy-remote.sh under $DEPLOY_DIR"; exit 1; }
            chmod +x "$script_path"
            bash "$script_path"

      - name: Post-deploy health probe
        if: "!inputs.dry_run"
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ inputs.host || secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          command_timeout: 30s
          command_stop: false   # health-check failure is a warning, not a hard error
          script: |
            set -euo pipefail
            for i in $(seq 1 15); do
              if curl -fsS --max-time 3 http://127.0.0.1:8888/ > /dev/null 2>&1; then
                echo "Frontend / on port 8888 OK."
                exit 0
              fi
              echo "Attempt $i: not ready, sleeping 2s..."
              sleep 2
            done
            echo "::warning::Frontend http://127.0.0.1:8888/ did not return 200 within 30s — deploy may be incomplete."